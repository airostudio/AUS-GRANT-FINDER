{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf .turbo node_modules/.cache && pnpm turbo build --filter=@ausgrant/web --force",
  "devCommand": "pnpm turbo dev --filter=@ausgrant/web",
  "installCommand": "pnpm install",
  "framework": null,
  "outputDirectory": "apps/web/.next"
}
